<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property name="catalinaBase" value="${catalina.base}" />
    <property name="logFileBase" value="./target/logs/tcui" />
    
    <if condition='!isNull("catalina.base")'>
        <then>
            <property name="logFileBase" value="${catalina.base}/logs/tcui" />
        </then>
    </if>

	<appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
		<layout class="ch.qos.logback.classic.PatternLayout">
			<Pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
			</Pattern>
		</layout>
	</appender>

    <appender name="FILEOUT"
              class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- roll-over nightly at midnight -->
            <fileNamePattern>${logFileBase}.%d.log</fileNamePattern>
        </rollingPolicy>
        <encoder>
            <Pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
            </Pattern>
        </encoder>
    </appender>

	<root level="INFO">
		<!--<appender-ref ref="STDOUT" />-->
		<appender-ref ref="FILEOUT" />
	</root>

    <logger name="ch.qos" level="INFO" />

    <logger name="org.springframework" level="INFO" />
    <logger name="org.apache" level="INFO" />
    <logger name="com.google" level="INFO" />
    <logger name="at.dimoco.dims" level="INFO" />

</configuration>
